name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { bundler-key: **urner** }
      - runz knx install
      - run: bun test
      - run: bun build
